#ifndef DATA_TYPES_H
#define DATA_TYPES_H

typedef struct settings_struct {
    char username[50];
    char password[50];
    int someSetting; // Example setting, can be expanded
} settings_t;

#endif // DATA_TYPES_H